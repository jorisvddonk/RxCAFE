# admin

Administrative access annotations.

## admin.authorized

| Property | Value |
|----------|-------|
| Type | `boolean` |

Indicates whether the client is authorized for admin operations. Used by the system agent to enforce access control.

**Usage:** Set on chunks from API clients that have been granted admin privileges via token.
